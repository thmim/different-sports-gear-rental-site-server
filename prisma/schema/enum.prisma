enum Role {
    CUSTOMER
    PROVIDER
    ADMIN
}

enum UserStatus {
   
    SUSPEND
    ACTIVE
}

enum RentalStatus {
    PENDING
    ACTIVE
    RETURNED
    CANCELLED
    CONFIRMED
}

enum ProductCondition {
    NEW
    LIKE_NEW
    GOOD
    FAIR
    POOR
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
    REFUNDED
}
